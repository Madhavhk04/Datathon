# Optional Google Gemini API Key
# If set, the AI Agent uses Google GenAI SDK (gemini-2.5-flash) with tool-calling loops.
# If omitted, the AI Agent runs locally with deterministic correlation rules over cleaned CSV data.
GEMINI_API_KEY=
